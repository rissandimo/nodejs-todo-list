<!DOCTYPE html>
<html lang="en">
    <%- include('./partials/header.ejs') %>
<body>
    <%- include('./partials/navbar.ejs') %>
    <a href="/todos/<%= todo._id %> ">Back to details page</a>
    <h2 class="header">Edit Todo</h2>
    <div class="edit-task">
        <form method="POST" action="/todos/:id?_method=PUT">
            <label for="title">Title</label>
            <input type="text" name="title" placeholder="<%=todo.title%>" >
            <label for="description">Description: </label>
            <textarea type="text" name="description" placeholder="<%= todo.description %>"></textarea>
            <input class="hidden" type="text" name="todo_id" value="<%= todo._id %>" >
            <button>Submit</button>
        </form>
    </div>
</body>
</html>